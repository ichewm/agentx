# GPT Model Profile

Status: profile-level guidance for GPT-family models. Verify current model-specific limits before making benchmark claims.

Sources:

- `https://platform.openai.com/docs/models`
- `https://developers.openai.com/codex`

## Scope

This profile affects expression guidance and review sensitivity only.

## Install Paths

Not applicable. Models are not installation targets.

## Role In AgentX

GPT-family models are expected to handle concise procedural skills well, but AgentX must still benchmark trigger behavior in the target runtime. Model profile guidance must not override target runtime profiles.
