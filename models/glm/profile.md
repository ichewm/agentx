# GLM Model Profile

Status: model-only profile. GLM is not an AgentX target runtime until a concrete GLM runtime with native capability installation is identified.

Sources:

- `https://docs.bigmodel.cn/`
- `https://www.z.ai/blog/glm-4.5`

## Scope

This profile affects expression guidance and review sensitivity only.

## Install Paths

Not applicable. GLM is not an AgentX installation target unless a concrete GLM runtime with native capability installation is identified.

## Role In AgentX

Use this profile only to tune generated capability wording or review sensitivity for GLM-family models. Do not create `targets/glm/` without verified runtime installation semantics.
