# OpenClaw Creator Baseline

Baseline: OpenClaw skill package.

Sources:

- `https://github.com/openclaw/openclaw`
- `https://docs.openclaw.dev/skills/overview`

Minimum package:

```text
<skill-id>/
  SKILL.md
```

`SKILL.md` must include metadata such as `name` and `description`, followed by instructions.
