# OpenClaw Install

Install methods:

- `official-cli`: use `openclaw skill install` when available.
- `export-only`: leave generated package under AgentX output.

The install plan must show the exact OpenClaw CLI command before running it.
