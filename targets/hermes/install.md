# Hermes Install

Install methods:

- `path-copy`: copy generated package to `~/.hermes/skills/<skill-id>/`.
- `manual`: use Hermes documented skill registration when configured differently.
- `export-only`: leave generated package under AgentX output.

Default mode is export-only unless the user confirms a Hermes skill directory.
