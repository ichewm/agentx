# GitHub Copilot Install

Install methods:

- `path-copy`: repository skill to `<repo>/.github/skills/<skill-id>/`.
- `path-copy`: personal skill to `~/.copilot/skills/<skill-id>/`.
- `export-only`: leave generated skill package in AgentX output.

Default mode is export-only until the user confirms repository or personal scope.
