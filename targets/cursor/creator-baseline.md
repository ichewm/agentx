# Cursor Creator Baseline

Baseline: Cursor project rule.

Source:

- `https://docs.cursor.com/context/rules`

Minimum artifact:

```text
.cursor/rules/<rule-id>.mdc
```

Rules are Markdown files with metadata/frontmatter and rule instructions. AgentX must use Cursor rule semantics rather than inventing a skill package for Cursor.
