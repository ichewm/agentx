# Cursor Review Checklist

- Verify `.mdc` frontmatter and rule type.
- Verify the rule is not too broad for Cursor routing.
- Verify the generated artifact lives under `.cursor/rules/`.
- Verify any manual setup is listed in the install plan.
- Verify no fake `SKILL.md` package is emitted for Cursor.
