# Cursor Target Profile

Status: verified for Cursor Rules.

Sources:

- `https://docs.cursor.com/context/rules`

## Runtime Capabilities

- Native skills: no target-native skill package is used in this profile.
- Rules: supported through `.cursor/rules/*.mdc`.
- Prompts: supported as rule content.
- Commands: not generated by default.
- Tools: not installed by rules.
- Connectors: not generated by default.
- Hooks: not generated by default.
- Subagents: not generated by default.

## Routing

- Runtime chooses capabilities: yes, according to rule type and metadata.
- Routing source: `.mdc` frontmatter and rule body.

## Scopes

- Project: `.cursor/rules/<rule-id>.mdc`.
- User: user rules are configured outside the generated project artifact.
- Organization: not represented by this profile.
- Plugin: not represented by this profile.

## Refresh Requirements

Cursor discovers project rules from `.cursor/rules/`. Runtime benchmark must verify visibility and trigger behavior.

## Unsupported Features

Do not generate fake skill packages for Cursor. Use `.mdc` project rules unless official Cursor docs provide another target-native capability format.
